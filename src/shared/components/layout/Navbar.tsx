'use client';
import { GraduationCap, House, ChartNoAxesColumnIncreasing, Plus, Pencil, Menu, X, UserRound, Settings } from 'lucide-react';
import { useState } from 'react';
export function Wordmark() { return <span className="wordmark" dir="ltr"><GraduationCap size={41} strokeWidth={1.7} /><span><b>BIG</b><small>EDUCATION</small></span></span>; }
export default function Navbar({ add, edit, canEdit, canAdd }: { add: () => void; edit: () => void; canEdit: boolean; canAdd: boolean }) { const [open, setOpen] = useState(false); const navigate = (id: string) => { document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' }); setOpen(false); }; return <><header className="mobile-header"><button className="icon-button" aria-label={open ? 'إغلاق القائمة' : 'فتح القائمة'} aria-expanded={open} onClick={() => setOpen(!open)}>{open ? <X /> : <Menu />}</button><Wordmark /><span className="profile-icon"><UserRound size={18} /></span></header><aside className={`sidebar ${open ? 'is-open' : ''}`}><div className="side-brand"><Wordmark /></div><nav aria-label="القائمة الرئيسية"><button className="nav-active" onClick={() => navigate('overview')}><House size={19} />الرئيسية</button><button onClick={() => navigate('progress')}><ChartNoAxesColumnIncreasing size={19} />إحصائيات</button><button disabled={!canAdd} onClick={() => { add(); setOpen(false); }}><Plus size={21} />إضافة سنة جديدة</button><button disabled={!canEdit} onClick={() => { edit(); setOpen(false); }}><Pencil size={18} />تعديل السنة</button><button disabled aria-disabled="true"><Settings size={18} />الإعدادات</button></nav><div className="sidebar-bottom"><GraduationCap size={112} strokeWidth={0.8} /><p dir="ltr">Better<br />Learning.<br />Brighter<br /><span>Future.</span></p></div></aside>{open && <button className="nav-backdrop" onClick={() => setOpen(false)} aria-label="إغلاق القائمة" />}</>; }
