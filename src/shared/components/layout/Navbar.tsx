'use client';
import { House, ChartNoAxesColumnIncreasing, Plus, Pencil, Menu, X, UserRound } from 'lucide-react';
import { useState } from 'react';
import Image from 'next/image';
import Link from 'next/link';
export function Wordmark() { return <span className="wordmark"><Image src="/image/test.png" alt="BIG EDUCATION" width={1672} height={941} priority /></span>; }
export default function Navbar({ add, edit, canEdit, canAdd }: { add: () => void; edit: () => void; canEdit: boolean; canAdd: boolean }) { const [open, setOpen] = useState(false); const navigate = (id: string) => { document.getElementById(id)?.scrollIntoView({ behavior: 'smooth' }); setOpen(false); }; return <><header className="mobile-header"><button className="icon-button" aria-label={open ? 'إغلاق القائمة' : 'فتح القائمة'} aria-expanded={open} onClick={() => setOpen(!open)}>{open ? <X /> : <Menu />}</button><Wordmark /><span className="profile-icon"><UserRound size={18} /></span></header><aside className={`sidebar ${open ? 'is-open' : ''}`}><div className="side-brand"><Wordmark /></div><nav aria-label="القائمة الرئيسية"><button className="nav-active" onClick={() => navigate('overview')}><House size={19} />الرئيسية</button><button onClick={() => navigate('progress')}><ChartNoAxesColumnIncreasing size={19} />إحصائيات الطالب</button><Link className="nav-link" href="/" onClick={() => setOpen(false)}><UserRound size={19} />تحليلات المعلمين</Link><button disabled={!canAdd} onClick={() => { add(); setOpen(false); }}><Plus size={21} />إضافة سنة جديدة</button><button disabled={!canEdit} onClick={() => { edit(); setOpen(false); }}><Pencil size={18} />تعديل السنة</button></nav><div className="sidebar-bottom"><Image src="/image/under.png" alt="BIG EDUCATION" width={1671} height={941} priority /></div></aside>{open && <button className="nav-backdrop" onClick={() => setOpen(false)} aria-label="إغلاق القائمة" />}</>; }
