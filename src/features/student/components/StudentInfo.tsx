import { CalendarDays, GraduationCap, UserRound } from 'lucide-react';
import type { Student } from '../types/student.types';
export default function StudentInfo({ student, count }: { student: Student; count: number }) { return <section className="student-info panel"><span className="avatar"><UserRound size={29} fill="currentColor" /></span><div><h1>{student.student_name}</h1><p>رحلتك الدراسية، في مكان واحد</p></div><div className="student-info-extra"><CalendarDays size={19} /><span>{count} سنوات دراسية</span></div><GraduationCap className="student-cap" size={66} strokeWidth={1.2} /></section>; }
