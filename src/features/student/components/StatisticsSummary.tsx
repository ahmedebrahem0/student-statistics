import { Star, Trophy, ChartNoAxesColumnIncreasing, TrendingUp } from 'lucide-react';
import type { AcademicYear } from '../types/student.types';
import { numberLabel, ratingLabel } from '../utils/student.utils';
export default function StatisticsSummary({ years }: { years: AcademicYear[] }) { const latest = years.at(-1); const previous = years.at(-2); const change = latest && previous ? latest.gpa - previous.gpa : null; return <section className="statistics" aria-label="ملخص الأداء"><article className="stat panel"><Star className="stat-icon" fill="currentColor" /><div><span className="stat-label">المعدل الحالي <b>GPA</b></span><div className="stat-value"><strong dir="ltr">{latest ? numberLabel(latest.gpa) : '—'}</strong>{change !== null && <span className={`change ${change < 0 ? 'negative' : ''}`}><TrendingUp size={12} /><b dir="ltr">{change > 0 ? '+' : ''}{numberLabel(change)}</b></span>}</div></div></article><article className="stat panel"><Trophy className="stat-icon" /><div><span className="stat-label">التقييم العام</span><strong className="rating-value">{latest ? ratingLabel(latest.overall_rating) : '—'}</strong></div></article><article className="stat panel"><ChartNoAxesColumnIncreasing className="stat-icon" /><div><span className="stat-label">عدد السنوات</span><strong className="years-value" dir="ltr">{years.length}<span>سنة</span></strong></div></article></section>; }
