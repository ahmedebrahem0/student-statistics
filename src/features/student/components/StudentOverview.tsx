'use client';
import { useState } from 'react';
import { CalendarDays, Plus, Pencil, TrendingUp, ChartNoAxesColumnIncreasing, GraduationCap, Check, ArrowLeft } from 'lucide-react';
import { useStudent } from '../hooks/useStudent';
import StudentInfo from './StudentInfo';
import StatisticsSummary from './StatisticsSummary';
import GradesLineChart from './charts/GradesLineChart';
import SubjectsBarChart from './charts/SubjectsBarChart';
import GradesRadarChart from './charts/GradesRadarChart';
import AddYearModal from './add-year/AddYearModal';
import Navbar from '@/shared/components/layout/Navbar';
import Footer from '@/shared/components/layout/Footer';
import Loader from '@/shared/components/common/Loader';
import ErrorMessage from '@/shared/components/common/ErrorMessage';
import EmptyState from '@/shared/components/common/EmptyState';
import { numberLabel, ratingLabel } from '../utils/student.utils';
export default function StudentOverview() {
  const { student, years, selected, select, saveYear, demo, toggleDemo, hasChanges, isLoading, isFetching, refetch } = useStudent();
  const [modal, setModal] = useState<'add' | 'edit' | null>(null);
  const [saved, setSaved] = useState(false);
  const add = () => { if (student) setModal('add'); };
  const edit = () => { if (selected) setModal('edit'); };
  return <div className="app-shell"><Navbar add={add} edit={edit} canEdit={Boolean(selected)} /><main className="dashboard" id="overview">{demo && <div className="demo-banner"><span>بيانات تجريبية — معاينة التصميم</span><button onClick={toggleDemo}>العودة للبيانات الفعلية<ArrowLeft size={14} /></button></div>}{!student ? isLoading ? <Loader /> : <ErrorMessage retry={() => { void refetch(); }} preview={toggleDemo} busy={isFetching} /> : <><StudentInfo student={student} count={years.length} /><StatisticsSummary years={years} /><section className="year-picker panel" aria-label="السنوات الدراسية"><div className="section-heading"><h2><CalendarDays size={17} />السنوات الدراسية</h2><button className="compact-button" onClick={add}><Plus size={16} />إضافة سنة جديدة</button></div>{years.length > 0 ? <div className="year-tabs" role="group" aria-label="اختيار السنة الدراسية">{years.map(year => <button key={year.year_id} dir="ltr" aria-pressed={selected?.year_id === year.year_id} className={selected?.year_id === year.year_id ? 'selected' : ''} onClick={() => select(year.year_id)}>{year.year_label}</button>)}</div> : <p className="muted small">لا توجد سنوات دراسية حتى الآن.</p>}</section>{years.length > 0 ? <><section className="chart-panel panel" id="progress"><div className="section-heading"><h2><TrendingUp size={16} />تطور المعدل الدراسي <span dir="ltr">(GPA)</span></h2><span className="chart-subtitle">كل سنة، خطوة للأمام</span></div><GradesLineChart years={years} /></section>{selected && <><div className="comparison-grid"><section className="chart-panel panel"><div className="section-heading"><h2><ChartNoAxesColumnIncreasing size={16} />مقارنة درجات المواد</h2><span dir="ltr" className="chart-subtitle">{selected.year_label}</span></div>{selected.subjects.length ? <SubjectsBarChart year={selected} /> : <EmptyState title="لا توجد مواد لهذه السنة" description="أضف المواد من تعديل السنة الدراسية." />}</section><section className="chart-panel panel radar-panel"><div className="section-heading"><h2>أداء المواد</h2><span className="legend"><i />{selected.year_label}</span></div>{selected.subjects.length >= 3 ? <GradesRadarChart year={selected} /> : <EmptyState title="مواد أكثر، صورة أوضح" description="يظهر توزيع الأداء عند وجود ثلاث مواد على الأقل." />}</section></div><div className="details-grid"><section className="details-panel panel"><div className="section-heading"><h2>تفاصيل المواد <span dir="ltr">{selected.year_label}</span></h2><button className="compact-button" onClick={edit}><Pencil size={14} />تعديل السنة</button></div>{selected.subjects.length ? <table className="subjects-table"><caption className="sr-only">درجات وتقديرات مواد السنة {selected.year_label}</caption><thead><tr><th scope="col">المادة</th><th scope="col">الدرجة</th><th scope="col">التقييم</th></tr></thead><tbody>{selected.subjects.map((subject, index) => <tr key={`${subject.name}-${index}`}><th scope="row"><i />{subject.name}</th><td dir="ltr">{numberLabel(Number(subject.score))}</td><td><span className={`rating-badge ${Number(subject.score) < 50 ? 'low-rating' : ''}`}>{ratingLabel(subject.rating)}</span></td></tr>)}</tbody></table> : <EmptyState title="لا توجد مواد لهذه السنة" description="اضغط تعديل السنة لإضافة المواد." />}<div className="details-summary"><span>معدل السنة</span><strong dir="ltr">{numberLabel(selected.gpa)}<small> / 100</small></strong></div></section><section className="add-year-panel panel"><button className="primary-button" onClick={add}><Plus size={18} />إضافة سنة جديدة</button><GraduationCap size={115} strokeWidth={0.9} /><p>سنة جديدة.<br /><strong>فرصة جديدة للتفوق.</strong></p><small dir="ltr">Your progress. Our priority.</small></section></div></>}</> : <section className="panel"><EmptyState /><button className="primary-button empty-add" onClick={add}><Plus size={18} />إضافة أول سنة دراسية</button></section>}{hasChanges && <p className="session-notice"><Check size={14} />تغييرات هذه الجلسة محلية ومؤقتة؛ تُحذف عند إعادة تحميل الصفحة.</p>}<Footer /><button className="floating-add" onClick={add} aria-label="إضافة سنة دراسية"><Plus size={26} /></button></>}{saved && <div className="save-toast" role="status"><Check size={18} />تم حفظ التغييرات في الجلسة الحالية<button className="text-button" onClick={() => setSaved(false)} aria-label="إغلاق إشعار الحفظ">×</button></div>}</main>{modal && student && <AddYearModal existing={modal === 'edit' ? selected : undefined} years={years} save={year => { saveYear(year); setSaved(true); }} close={() => setModal(null)} />}</div>;
}
