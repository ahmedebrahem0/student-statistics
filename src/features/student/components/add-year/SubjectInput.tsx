import { Trash2 } from 'lucide-react';
import type { UseFormRegister } from 'react-hook-form';
import type { YearFormValues } from '../../schema/student.schema';
export default function SubjectInput({ index, register, remove, canRemove, nameError, scoreError }: { index: number; register: UseFormRegister<YearFormValues>; remove: () => void; canRemove: boolean; nameError?: string; scoreError?: string }) { return <div className="subject-input"><label>اسم المادة<input {...register(`subjects.${index}.name`)} placeholder="مثال: الرياضيات" aria-invalid={Boolean(nameError)} />{nameError && <span className="field-error">{nameError}</span>}</label><label>الدرجة<input {...register(`subjects.${index}.score`)} type="number" inputMode="decimal" step="0.01" min="0" max="100" placeholder="0 – 100" dir="ltr" aria-invalid={Boolean(scoreError)} />{scoreError && <span className="field-error">{scoreError}</span>}</label><button type="button" className="icon-button remove-subject" aria-label={`حذف المادة ${index + 1}`} disabled={!canRemove} onClick={remove}><Trash2 size={17} /></button></div>; }
