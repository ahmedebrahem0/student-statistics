import type { UseFormRegister } from 'react-hook-form';
import type { YearFormValues } from '../../schema/student.schema';
export default function SubjectInput({ index, name, register, scoreError }: { index: number; name: string; register: UseFormRegister<YearFormValues>; scoreError?: string }) { return <div className="subject-input"><label className="subject-name-label" htmlFor={`score-${index}`}>{name}</label><div><input id={`score-${index}`} {...register(`grades.${index}.score`)} type="number" inputMode="decimal" step="0.01" min="0" max="100" placeholder="0 – 100" dir="ltr" aria-invalid={Boolean(scoreError)} />{scoreError && <span className="field-error">{scoreError}</span>}</div></div>; }
